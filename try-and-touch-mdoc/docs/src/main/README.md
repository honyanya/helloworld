# mdoc

scala

```scala
val hoge = "hoge"
```

scala mdoc

```scala mdoc
val hoge = "hoge"
```

case class

```scala mdoc
case class Person(name: String, age: Int)

val person = Person("hoge", 20)

person.name
person.age
```
